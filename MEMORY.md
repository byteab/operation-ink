# Project environment

- Annotated map fixes (2026-09-18): removed outer masonry wall, moved west utility north 3m, removed pine at plan (553,590), moved inner gate to (667,555), fenced water-tower rail shortcut and observation/admin gaps. Zipline now water→observation only (desktop/VR). Jeep uses neutral pen outlines and opaque white paper surfaces; user clarified that transparent wireframe was not intended. AI plans around open leaves and recovers door overlaps. Build/player/map/rescue/AI/expansion checks pass; agent-browser evidence and route details in `docs/map-annotations/README.md`.

- Single-hostage visual revision (2026-09-18, latest): one green `stickman.glb` prisoner in cell 01, seated on a chair behind bars. `hostage-actor.ts` shares enemy rig/skinning and adds seated/stand-up/sit-down clips; escort waits for standing and boards continuously at the smaller Willys jeep's passenger side. `rescue-jeep.ts` owns compact vehicle geometry, recessed footwells and rotating wheels. Mission/HUD require one hostage. `test:rescue` now has 26 checks; build, player/map and enemy-motion checks pass. Staged headless F-release/escort/escape/restart plus desktop/mobile screenshots are documented in `docs/hostage-rescue/REVISION-SINGLE-HOSTAGE.md`. Prior four-hostage completion logs below are historical, not evidence for this revision.

- Hostage rescue revision (2026-09-18): `/` is now Operation Safe Return. Shared map coordinates live in `src/game/rescue-layout.ts`; `mission.ts` is authoritative serializable state, runtime owns effects, `hostages.ts` follows a collision-checked basement/stair/jeep route, and `security.ts` feeds finite camera sightings/alarms into existing AI. Four protected hostages, four capped reserves, dynamic exit gate, scripted jeep departure and insertion retry. Run `npm run test:rescue` (24 focused checks). Design, agent ownership, independent review and browser evidence: `docs/hostage-rescue/`. Input-only fresh-start combat completion recorded in `evidence/input-completion.json`; separate staged diagnostics explicitly documented. Existing dirty user work preserved. Dev server for this session uses http://127.0.0.1:5174 because 5173 was already occupied.

Environment inspected 2026-09-16 by the repository reconnaissance specialist. Source-authoritative JSON and setup details are preserved in [docs/first-level/RECON.md](docs/first-level/RECON.md).

- Vite / TypeScript / Three.js / Preact web project. `is_react_native=false`, `is_native_ios=false`, `is_native_android=false`; no Metro.
- `npm run dev` (5173); `npm run build`; player, VR, mission, weapons, map and AI checks in package.json.
- `/` is Last Green Light. `/?explore=1` preserves the original exploration/experimental VR view. `/lab.html` is the independent character lab.
- User explicitly requested agent-browser for browser verification. Cached executable in this environment: `node /Users/ehsan/.npm/_npx/6de2aa2fded2970c/node_modules/agent-browser/bin/agent-browser.js` (0.27.0).
- First-level design, contracts, ownership, handoffs and evidence live in `docs/first-level/`. Preserve the pre-existing uncommitted VR/controller changes noted in RECON.md.
- Gameplay polish ownership, shared damage configuration, handoffs and evidence: `docs/gameplay-polish/`. `npm run test:polish` covers sniper/reset, AI/reactions, audio lifecycle and effects. Map has one water tower and one observation tower; a stationary sniper occupies each. Maintenance supplies include the player sniper.
- Headless agent-browser sessions use `AGENT_BROWSER_SOCKET_DIR=/tmp/stickman-browser`; local server/browser processes require sandbox escalation in this environment. Use unique session names and close only sessions started for your task.
- Combat expansion: 40 active guards plus four reserves, including 20 new indoor placements; bidirectional tower cable uses F; fence wire blocks bodies but permits sight/shots. Near misses trigger scanning, blood uses cached regional collision queries, confirmed hits have local audio. `npm run test:expansion` covers these systems. Independent agent-browser results: [docs/combat-expansion/evidence/verification-report.md](docs/combat-expansion/evidence/verification-report.md).
- Ladder/audio revision: stair camera follows a continuous incline; ladder entry preserves orientation, descending entry has no upward clearance bump, and ladder playback was silenced in that revision (re-enabled with original IGI clips below). Guard pain is a separately bounded audio event; dialogue assets regenerated at correct source rates, with credited recorded Hey/pain additions. `npm run test:traversal-audio`; evidence in `docs/ladder-audio/`.
- IGI sound import: `scripts/extract-igi-audio.py` extracts 333 common PCM effects to ignored `artifacts/igi-audio.local/` and installs 52 selected WAVs to `public/sounds/igi/`. `src/game/igi-samples.ts` maps 25 events and three dialogue groups; prior samples/synthesis remain fallback. Original IGI ladder one-shots and detection/hurt vocals are enabled. `python3 scripts/igi-audio-checks.py` checks archive integrity; routing/fallback tests are in polish audio checks. Source manifest and documentation: `docs/igi-audio/README.md`.
- Character audio is now IGI-only: generated `voice_*`, `guard_hey`, and `pain_*` recordings were moved out of public assets to ignored `artifacts/removed-character-audio.local/`. Unmapped/unavailable callouts retain captions but produce no sound; only non-vocal effects retain older samples/synthesis as fallback.

- Responsive but bounded enemy AI: direct nearby sight enters combat with a minimum 800ms aim delay before firing, also on reacquisition (160ms planted readiness); passive ranges are 20m guards / 28m snipers. Long-range sight (60m / 110m) requires personal contact, a hit or a visible muzzle, expires after 8s without sight, and is not granted by radio/hearing. Heard noises and near misses use investigation captions, never detection vocals without sight. Accuracy/cadence eased after user feedback; tracking, cover/flanks, exposed-head targeting and spatial flybys remain. Mission starts behind the mess hall north wall at [-40, 0.15, -62.3], with a walkable west route to the ladder. `test:ai` includes response and fair-awareness regressions. Research, browser evidence and test details: `docs/enemy-ai/README.md`. Use agent-browser as explicitly requested.

- Starter loadout: four slots (1 pistol 12/36, 2 shotgun 6/24, 3 AK 30/90, 4 SMG 24/72), no sniper. Shotgun uses the existing model, eight spread pellets, distance falloff, pump/support-hand movement and interruptible per-shell reloads. `test:weapons` includes `starting-loadout-checks.ts`; agent-browser evidence in `docs/enemy-ai/evidence/starting-loadout-results.json`. Three guards nearest the starting ladder removed (west aisle and two signals-office guards); current roster is 37 active + four reserves, with no authored spawn/patrol within 12m of ladder.
